import { defineMessages } from "react-intl";

export default defineMessages({
  searchResults: {
    id: "book.binge.mainContainer.searchResults",
    defaultMessage: "Search results",
  },
});