import { defineMessages } from "react-intl";

export default defineMessages({
  placeholderText: {
    id: "book.binge.searchBanner.placeholderText",
    defaultMessage: "Search for a book by title, author, or keyword...",
  }
});